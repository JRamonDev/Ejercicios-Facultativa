/*Escribe una función que tome dos números como parámetros y devuelva el
número menor. */
var miFuncion1 = function (parametro1, parametro2) {
    return (Math.min(parametro1,parametro2))
}
console.log(miFuncion1(10,20));
