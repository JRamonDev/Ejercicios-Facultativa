/*Escribe una función que tome un arreglo como parámetro y devuelva el último
elemento del arreglo. */
function ultimo(arreglo){
    return arreglo[arreglo.length -1];
}
console.log (ultimo('Javier'));