/*Escribe una función que tome tres números como parámetros y devuelva el
resultado de la suma de los dos primeros, multiplicado por el tercero. */
function suma(a,b,j){
return (a+b)*j;
}
console.log(suma(10,20,30));