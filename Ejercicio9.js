/*Escribe una función que tome un arreglo como parámetro y devuelva el
número de elementos en el arreglo. */
function numelementos(arreglo) {
    return arreglo.length;

  }
console.log(numelementos([8,12,14,6]))
