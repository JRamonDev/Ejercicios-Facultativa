/*Escribe una función que tome un arreglo de strings como parámetro y
devuelva un nuevo arreglo con todas las palabras en minúscula. */
function convertirArreglo(arreglo){
    let arregloNuevo=[];
    for(let i= 0; i<arreglo.length; i++){
        nuevoArr.push(arr[i].toLoweCase());
    }
    return NuevoArr;
}
console.log(convertirArreglo("Hola"));